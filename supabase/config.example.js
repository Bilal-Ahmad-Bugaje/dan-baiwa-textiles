// Copy this file to supabase/config.js for local testing only.
// In production, inject these values through your Vercel environment/configuration.
// The publishable key is safe for browser use only when RLS is correctly configured.
// NEVER put a Supabase service-role/secret key here.
window.DAN_BAIWA_SUPABASE = {
  url: 'https://YOUR-PROJECT.supabase.co',
  publishableKey: 'YOUR_SUPABASE_PUBLISHABLE_KEY'
};
